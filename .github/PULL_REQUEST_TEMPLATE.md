## Summary

<!-- What and why, one short paragraph -->

## Checklist

- [ ] `bun test` passes
- [ ] `bunx tsc --noEmit` clean
- [ ] Golden fixtures updated if author output changed (`examples/*/AGENTS.md`)
- [ ] Root `AGENTS.md` refreshed if detectors/author changed (`bun run src/cli.ts`)
- [ ] Doctrine still holds: no `generate` wording; no FAF footprint in authored MD
- [ ] CHANGELOG.md updated if user-facing

## Governing filter

Every new authored line resolves **ambiguity** or **expensive exploration** — no filler.
